#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of string
 * @s: a pointer
 * Return: 0
 */

size_t _strlen(const char *s)
{
	size_t length = 0;

	while (*str++)
		length++;
	return (length);
}
