#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9).
 * @c: value to be checked.
 * Return: Return 1 if digit and 0 if otherwise.
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
