#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Program to print all single decimals of base 10 from 0'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
